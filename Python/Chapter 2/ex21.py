"""
    1. Every class will have their unique fields and methods eventhough some of them might be the same for other classes
        1.1 Which will use more spaces because those methods will be created at that class's namespace for each
    2. Each class might be big as hell because there's not much abstraction made
    3. There might be some classes which are identical in someway
    4. We dont know which class to use while we re-use those classes

"""
